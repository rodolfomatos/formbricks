import { LinkSurveyNotFound } from "@/modules/survey/link/not-found";

/**
 * 404 page for the `/s/[surveyId]` link survey route.
 */
export default function NotFound() {
  return <LinkSurveyNotFound />;
}
