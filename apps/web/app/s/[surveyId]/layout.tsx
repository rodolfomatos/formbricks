import { LinkSurveyLayout, viewport } from "@/modules/survey/link/layout";

/**
 * Layout for `/s/[surveyId]` link survey pages. Provides the
 * unauthenticated survey-taking UI shell.
 */
export { viewport };

export default LinkSurveyLayout;
