import { LinkSurveyPage, generateMetadata } from "@/modules/survey/link/page";

/**
 * Route: `/s/[surveyId]` (unauthenticated). Public link survey page where
 * respondents fill out and submit the survey.
 */
export { generateMetadata };
export default LinkSurveyPage;
