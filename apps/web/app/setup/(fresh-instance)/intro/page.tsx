import { IntroPage, metadata } from "@/modules/setup/(fresh-instance)/intro/page";

/**
 * Route: `/setup/intro` (unauthenticated, fresh-instance). Welcome screen for
 * first-time instance setup.
 */
export { metadata };
export default IntroPage;
