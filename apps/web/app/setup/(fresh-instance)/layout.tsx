import { FreshInstanceLayout } from "@/modules/setup/(fresh-instance)/layout";

/**
 * Layout for `/setup/*` fresh-instance routes. Provides the branding shell
 * for first-time setup pages.
 */
export default FreshInstanceLayout;
