import { SignupPage, metadata } from "@/modules/setup/(fresh-instance)/signup/page";

/**
 * Route: `/setup/signup` (unauthenticated, fresh-instance). Admin account
 * creation for the initial instance setup.
 */
export { metadata };
export default SignupPage;
