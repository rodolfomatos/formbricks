import { InvitePage, metadata } from "@/modules/setup/organization/[organizationId]/invite/page";

/**
 * Route: `/setup/organization/[organizationId]/invite` (authenticated).
 * Invite team members during the setup wizard.
 */
export { metadata };
export default InvitePage;
