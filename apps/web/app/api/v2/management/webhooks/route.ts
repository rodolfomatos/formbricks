import { GET, POST } from "@/modules/api/v2/management/webhooks/route";

/**
 * GET  /api/v2/management/webhooks — Re-exported from modules. Lists webhooks.
 * POST /api/v2/management/webhooks — Re-exported from modules. Creates a webhook.
 */
export { GET, POST };
