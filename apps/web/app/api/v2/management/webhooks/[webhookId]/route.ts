import { DELETE, GET, PUT } from "@/modules/api/v2/management/webhooks/[webhookId]/route";

/**
 * GET   /api/v2/management/webhooks/[webhookId] — Re-exported from modules. Gets a webhook.
 * PUT   /api/v2/management/webhooks/[webhookId] — Re-exported from modules. Updates a webhook.
 * DELETE /api/v2/management/webhooks/[webhookId] — Re-exported from modules. Deletes a webhook.
 */
export { GET, PUT, DELETE };
