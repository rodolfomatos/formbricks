import { GET } from "@/modules/api/v2/management/surveys/[surveyId]/contact-links/contacts/[contactId]/route";

/**
 * GET /api/v2/management/surveys/[surveyId]/contact-links/contacts/[contactId]
 * Re-exported from modules. Returns contact-link relationship for a given survey+contact.
 */
export { GET };
