import { GET, POST } from "@/modules/api/v2/management/contact-attribute-keys/route";

/**
 * GET  /api/v2/management/contact-attribute-keys — Re-exported from modules. Lists contact attribute keys.
 * POST /api/v2/management/contact-attribute-keys — Re-exported from modules. Creates a contact attribute key.
 */
export { GET, POST };
