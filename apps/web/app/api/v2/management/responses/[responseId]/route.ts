import { DELETE, GET, PUT } from "@/modules/api/v2/management/responses/[responseId]/route";

/**
 * GET   /api/v2/management/responses/[responseId] — Re-exported from modules. Gets a response.
 * PUT   /api/v2/management/responses/[responseId] — Re-exported from modules. Updates a response.
 * DELETE /api/v2/management/responses/[responseId] — Re-exported from modules. Deletes a response.
 */
export { GET, PUT, DELETE };
