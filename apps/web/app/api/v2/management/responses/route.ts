import { GET, POST } from "@/modules/api/v2/management/responses/route";

/**
 * GET  /api/v2/management/responses — Re-exported from modules. Lists responses.
 * POST /api/v2/management/responses — Re-exported from modules. Creates a response.
 */
export { GET, POST };
