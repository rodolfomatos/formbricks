import { GET, PATCH, POST } from "@/modules/api/v2/organizations/[organizationId]/users/route";

/**
 * GET   /api/v2/organizations/[organizationId]/users — Re-exported from modules.
 * POST  /api/v2/organizations/[organizationId]/users — Re-exported from modules.
 * PATCH /api/v2/organizations/[organizationId]/users — Re-exported from modules.
 */
export { GET, POST, PATCH };
