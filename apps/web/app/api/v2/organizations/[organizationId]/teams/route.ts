import { GET, POST } from "@/modules/api/v2/organizations/[organizationId]/teams/route";

/**
 * GET  /api/v2/organizations/[organizationId]/teams — Re-exported from modules. Lists teams.
 * POST /api/v2/organizations/[organizationId]/teams — Re-exported from modules. Creates a team.
 */
export { GET, POST };
