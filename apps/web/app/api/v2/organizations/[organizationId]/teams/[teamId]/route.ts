import { DELETE, GET, PUT } from "@/modules/api/v2/organizations/[organizationId]/teams/[teamId]/route";

/**
 * GET   /api/v2/organizations/[organizationId]/teams/[teamId] — Re-exported from modules.
 * PUT   /api/v2/organizations/[organizationId]/teams/[teamId] — Re-exported from modules.
 * DELETE /api/v2/organizations/[organizationId]/teams/[teamId] — Re-exported from modules.
 */
export { GET, PUT, DELETE };
