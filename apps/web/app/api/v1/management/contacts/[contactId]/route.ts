import { DELETE, GET } from "@/modules/ee/contacts/api/v1/management/contacts/[contactId]/route";

/**
 * GET  /api/v1/management/contacts/[contactId] — Re-exported from EE module. Returns a single contact.
 * DELETE /api/v1/management/contacts/[contactId] — Re-exported from EE module. Deletes a contact.
 */
export { DELETE, GET };
