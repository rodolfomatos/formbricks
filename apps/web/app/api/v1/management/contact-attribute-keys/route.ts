import { GET, POST } from "@/modules/ee/contacts/api/v1/management/contact-attribute-keys/route";

/**
 * GET  /api/v1/management/contact-attribute-keys — Re-exported from EE module. Lists contact attribute keys.
 * POST /api/v1/management/contact-attribute-keys — Re-exported from EE module. Creates a new contact attribute key.
 */
export { GET, POST };
