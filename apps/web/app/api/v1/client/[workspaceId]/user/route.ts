import { OPTIONS, POST } from "@/modules/ee/contacts/api/v1/client/[workspaceId]/user/route";

/**
 * POST  /api/v1/client/[workspaceId]/user — Re-exported from EE module. Creates/updates a contact user.
 * OPTIONS — CORS preflight handler.
 */
export { POST, OPTIONS };
