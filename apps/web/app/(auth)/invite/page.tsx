import { InvitePage } from "@/modules/auth/invite/page";

/**
 * Route: `/invite` (unauthenticated). Accepts a workspace/organization invite
 * via query token and redirects through signup or login.
 */
export default InvitePage;
