import { ResetPasswordPage } from "@/modules/auth/forgot-password/reset/page";

/**
 * Route: `/auth/forgot-password/reset` (unauthenticated). Form to set a new
 * password using a reset token.
 */
export default ResetPasswordPage;
