import { SurveyEditorLoading } from "@/modules/survey/editor/loading";

/**
 * Loading fallback for the survey editor page.
 * Renders skeleton placeholders while the editor loads survey data.
 */
export default SurveyEditorLoading;
