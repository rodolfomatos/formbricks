import { SurveyTemplatesPage } from "@/modules/survey/templates/page";

/**
 * Route: `/workspaces/[workspaceId]/surveys/templates` (survey editor).
 * Displays the survey template gallery for creating a new survey from a template.
 */
export default SurveyTemplatesPage;
