import { SurveyEditorPage } from "@/modules/survey/editor/page";

/**
 * Route: `/workspaces/[workspaceId]/surveys/[surveyId]/edit` (survey editor).
 * Loads the full survey editor page for authoring and configuring a survey.
 */
export default SurveyEditorPage;
