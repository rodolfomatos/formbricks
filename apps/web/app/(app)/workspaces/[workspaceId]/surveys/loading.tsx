import { SurveyListLoading } from "@/modules/survey/list/loading";

/**
 * Loading fallback for the surveys list page.
 * Renders skeleton placeholders while survey data loads.
 */
export default SurveyListLoading;
