import { WorkspaceSettingsPage } from "@/modules/workspaces/settings/page";

/**
 * Route: `/workspaces/[workspaceId]/settings/workspace` (authenticated).
 * Workspace settings root page, delegates to the shared workspace settings module.
 */
export default WorkspaceSettingsPage;
