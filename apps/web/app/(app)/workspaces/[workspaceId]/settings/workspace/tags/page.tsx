import { TagsPage } from "@/modules/workspaces/settings/tags/page";

/**
 * Route: `/workspaces/[workspaceId]/settings/workspace/tags` (authenticated).
 * Manages survey response tags for the workspace.
 */
export default TagsPage;
