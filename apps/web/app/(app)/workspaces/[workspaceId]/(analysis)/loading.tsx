import { AnalysisListLoading } from "@/modules/ee/analysis/loading";

/**
 * Loading fallback for the analysis route group `(analysis)`.
 * Renders skeleton placeholders while dashboard/chart pages fetch data.
 */
export default AnalysisListLoading;
