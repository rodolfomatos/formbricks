import { LoadingSpinner } from "@/modules/ui/components/loading-spinner";

/** Loading state for the survey templates page. */
export const SurveyTemplatesLoading = () => {
  return <LoadingSpinner />;
};
