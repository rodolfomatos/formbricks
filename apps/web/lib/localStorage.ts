/** Key for persisting survey-list filter state in localStorage. Consumed by the survey overview page. */
export const FORMBRICKS_SURVEYS_FILTERS_KEY_LS = "formbricks-surveys-filters";
/** Key for caching the active environment ID across full-page reloads. */
export const FORMBRICKS_ENVIRONMENT_ID_LS = "formbricks-environment-id";
/** Key for caching the active workspace ID across full-page reloads. */
export const FORMBRICKS_WORKSPACE_ID_LS = "formbricks-workspace-id";
/** Key for remembering which auth method the user last logged in with (SSO provider name). */
export const FORMBRICKS_LOGGED_IN_WITH_LS = "formbricks-logged-in-with";
