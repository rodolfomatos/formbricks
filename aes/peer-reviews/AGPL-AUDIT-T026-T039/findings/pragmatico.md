[MAJOR] Neutered upgrade-prompt module dropped `ModalButton` export; 5 consumers import it, hidden by `typescript.ignoreBuildErrors`
Evidence: `grep -rn "ModalButton" apps/web/modules/ui/components/upgrade-prompt/` exits 1 (no match). `index.tsx:27` exports only `UpgradePrompt`. Five files import the missing name: `apps/web/app/(app)/workspaces/[workspaceId]/components/MainNavigation.tsx:58`, `workspace-breadcrumb.tsx:21`, `settings/organization/general/components/AISettingsToggle.tsx:16`, `modules/workspaces/components/workspace-limit-modal/index.tsx:5`, `modules/organization/settings/teams/components/invite-member/bulk-invite-tab.tsx:15` (used as tuple type `[ModalButton, ModalButton]`). The stub causing this is in audited commit a89a7d527 (git log --follow index.tsx → a89a7d527), which claims "zero dead upgrade-prompt consumers". The only reason this is not an error is `typescript.ignoreBuildErrors: true` (apps/web/next.config.mjs:45): any `tsc --noEmit` fails with "no exported member 'ModalButton'".
Closure condition: Re-export a `ModalButton` placeholder (or convert the 5 value imports to `import type`/drop them) AND `pnpm exec tsc --noEmit` in apps/web reports no error for these 5 files.
Rubric criterion: C-05

[MINOR] C-01 check as pre-registered does not return 0 lines: `ee.formbricks.com` survives in a code comment
Evidence: `grep -rn "formbricks.com" apps/web/modules/response-pipeline/lib/telemetry.ts` returns 1 line: `6: * The original Formbricks code POSTed usage telemetry to https://ee.formbricks.com`. The function itself is verified a no-op (`export const sendTelemetryEvents = async () => { logger.debug(...) }`, telemetry.ts:12-14) and C-02 (vitest modules/response-pipeline/lib/telemetry.test.ts) passes — but the pre-registered C-01 grep contract yields 1 line, not 0, so the audit's own gate records a false failure.
Closure condition: Rewrite the comment to drop the literal domain (e.g., "the upstream SaaS") so the C-01 grep returns 0; or amend the check to exclude comment lines and re-hash the rubric.
Rubric criterion: C-01

[MINOR] D-04 check as pre-registered returns 9 lines, not 0: relative `../../modules/` imports still exist (all resolve)
Evidence: exact check `grep -rn "modules/" apps/web --include="*.ts*" | grep -v "@/modules\|node_modules"` returns 9 lines including live relative imports `apps/web/lib/tag/service.ts:15` (`../../modules/workspaces/settings/types/tag`), `apps/web/lib/utils/logger-helpers.test.ts:101,139` (`../../modules/ee/audit-logs/lib/handler`), plus test comments/describe strings. Spot-checked: `apps/web/modules/workspaces/settings/types/tag.ts:1` exports `TagError`, handler.ts:75 exports `withAuditLogging` — all resolve, so no runtime defect, but the criterion claims zero `../../modules/` legacy imports while the pre-registered check output is non-zero.
Closure condition: Convert the live relative imports to the `@/modules` alias (or narrow the check to ignore comment/string lines) and re-run the D-04 grep → 0 functional matches.
Rubric criterion: D-04