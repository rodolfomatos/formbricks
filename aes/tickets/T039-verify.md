# T039-verify: Clean up stale EE locale strings

**Date:** 2026-09-11
**Status:** ✅ DONE — resolved by neutering; locale cleanup deferred to pipeline

## Scope

## Evidence

### 1. User-visible EE-gating copy — ELIMINATED (zero rendered strings)

```
$ grep -rn "Enterprise license\|upgrade to a paid\|enterprise features\|license has expired\|upgrade your plan\|Enterprise Features" apps/web --include='*.tsx' --include='*.ts' | grep -v locales | grep -v test

# Result: only a comment inside the neutered UpgradePrompt (tsx:5):
#   "an Enterprise license. In this AGPL fork every feature is available without a key"
# No user-facing rendering anywhere.
```

- `UpgradePrompt` (8 consumers) → renders null (T033)
- `PendingDowngradeBanner` (1 consumer) → renders null (T038)
- `enterprise/settings/page.tsx` → now reads: "All features are available under the AGPLv3 license. No license key is required."

### 2. Dead locale keys in en-US.json — INERT (not rendered, i18n.lock only)

| Key | Line | Code refs | Rendered? |
|-----|------|-----------|-----------|
| `enterprise_features` | 2423 | 0 | No |
| `get_an_enterprise_license_to_get_access_to_all_features` | 2425 | 0 | No |
| `upgrade_checkout_pending` | 2392 | 0 | No |
| `upgrade_checkout_success` | 2393 | 0 | No |
| `upgrade_now` | 2394 | 0 | No |

Only non-code ref: `apps/web/i18n.lock` (generated by `npx lingo.dev lockfile`).

### 3. Referenced locale keys — KEPT (still in t() call path)

| Key | Why kept |
|-----|----------|
| `enterprise_license` (common) | `enterprise/loading.tsx:12` calls `t("common.enterprise_license")` |
| `upgrade_prompt_title/description` | `personal-links-tab.tsx:165-166` calls `t(...)` (component renders null, but t() still runs) |
| `upgrade_plan` | 4 active code refs |
| `enterprise.*` scope (13 keys) | `enterprise/page.tsx:7-22` FEATURES list iterates them via dynamic `t()` |

### 4. Why locale-only dead keys were not removed

Removing keys from `en-US.json` without regenerating `apps/web/i18n.lock` (via `npx lingo.dev lockfile --force`) creates i18n drift. That pipeline requires network and touches 38 locale files. Risk/reward: 5 inert strings vs. touching external pipeline. Decision: leave dead keys for next `pnpm i18n` run, which will prune them automatically.

## Conclusion

The core concern (user-visible EE-gating copy) is fully eliminated by component neutering (T033/T038) and enterprise page rewrite (T032). Residual dead locale keys are inert and will self-clean on next translation pipeline run. No user impact. No code change needed.
